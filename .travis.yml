language: node_js
node_js: 
  - "stable"
env:
  - NODE_ENV="postgres://postgres@127.0.0.1:5432/travis_ci_test"
services:
  - postgresql
install:
  - npm ci
cache:
  directories:
  - "$HOME/.npm"
before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
  - psql -f ./testdb/teamwork.sql -d travis_ci_test -U postgres
script:
  - npm test